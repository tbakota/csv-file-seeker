# CSV file seeker
CSV goes in, files come out

After running csv-file-seeker.exe an .env file will be generated, fill out the required variables and run the .exe again.
For more details look inside the generated output.log
